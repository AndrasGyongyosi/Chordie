package com.example.ChordCalculator.Model.Rule;

public enum CatchPerfection {
    LOW,    // Not all notes.
    MEDIUM, // All notes, root note is bad.
    HIGH    // All notes, root note is fine.
}
